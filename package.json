{
  "name": "velync",
  "version": "1.0.0",
  "description": "Velync — SaaS Integration Platform. Prebuilt sync apps for your tools.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/unit.test.js",
    "test:rules": "npx firebase emulators:exec --only firestore \"node --test test/firestore-rules.test.js\"",
    "test:engine": "npx firebase emulators:exec --only firestore \"node --test test/engine.test.js\"",
    "test:all": "npm test && npm run test:rules && npm run test:engine"
  },
  "dependencies": {
    "@google-cloud/firestore": "^8.6.0",
    "@google/genai": "^2.10.0",
    "@notionhq/client": "^5.22.0",
    "axios": "^1.18.1",
    "cron-parser": "^5.6.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "express-validator": "^7.3.2",
    "firebase-admin": "^14.0.0",
    "form-data": "^4.0.6",
    "helmet": "^8.2.0",
    "node-cron": "^4.5.0",
    "nodemailer": "^9.0.1",
    "stripe": "^17.0.0",
    "uuid": "^14.0.1"
  },
  "overrides": {
    "jose": "^5.0.0"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1"
  }
}
